// p01_student.c
#include "stack.h"
#include <stddef.h>
// For each index i in a[0..n-1], write into out[i] the next greater
// element to the RIGHT of i, or -1 if it doesn’t exist.
void next_greater(const int *a, size_t n, int *out){
// TODO: Implement in O(n) time using a monotonic stack.
}

