// p07_student.c
#include "ll.h"

#include <stdbool.h>
    // Insert val at index pos in the list whose head pointer is *head.
    // Return true on success, false if pos is out of range.
    // On success, *head may change (e.g. inserting at position 0).
    bool ll_insert_at(Node **head, size_t pos, int val)
{
    // TODO: implement insertion at position pos.
    return false;
}